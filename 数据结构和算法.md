# 1数据结构



## 1.1 时间复杂度和空间复杂度

## 1.2 线性表

***线性表的定义*第一个元素无前驱元素,而最后一个元素无后继元素,其他元素都有且只有一个前驱元素和后继元素***

### 1.2.1 线性表的顺序存储结构

#### 1.2.1.1 `数组`

#### 1.2.1.2 静态链表

**静态链表由数组结构组成，里面维护两个链表,下标等于内存地址,游标等于下一个元素的内存地址p->next**

**下标0的游标指向的是为使用内存的起始地址,如0->6->7->...**

**最后一个下标元素指向的是已经使用的链表的起始地址,如 1->5->2->3->4->null, 0表示链表已经结尾了** 

![Snipaste_2025-10-04_21-28-11.png](.\image\Snipaste_2025-10-04_21-28-11.png)

### 1.2.2 线性表的链式存储结构

#### 1.2.2.1 单向链表



#### 1.2.2.3 栈和队列

> 栈: 栈的元素必须后进先出,栈的操作只能在表尾(栈顶)进行操作

# 2 算法

## 2.1 快慢指针

**用来快速查找未知长度单向链表的中间节点,也可以用来判断链表中是否有环(有环的情况下,快指针转了圈回来后总会追上慢指针)**

定义两个变量,一个变量步长为1,一个变量步长为2,当步长为2的指针走到结尾时,步长为1的指针正好在中间

![Snipaste_2025-10-04_21-38-45.png](.\image\Snipaste_2025-10-04_21-38-45.png)

## 2.2  环形链表的环头

外循环p每次只走一格,内循环q每次走到p的位置

如果p和q相等而且步长想等,那说明q追上了p, p向前走一步. q又重头开始和p比较

如果p和q相等并且步长不相等,那说明p是走到了链表的最后又循环回来的,而q每次都是从头开始并且不超过p的步长,此时q的步长所在的节点就是循环链表的头节点

![Snipaste_2025-10-05_15-06-23.png](.\image\Snipaste_2025-10-05_15-06-23.png)



