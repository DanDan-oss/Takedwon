## 未看跳过的章节

```c
cpp2019 --> 5大章:linux高并发 --> 04章:gdb_文件I/O_Linux系统函数
    22节:读指定目录下....
cpp2019 --> 5大章:linux高并发 --> 05-进程
    07, 08, 09
cpp2019 --> 5大章:linux高并发 --> 06-进程间的通信
    04
cpp2019 -->  8大章:分布式游戏服务器开发 --> 09-通道层-责任链
    06,07
cpp2019 -->  8大章:分布式游戏服务器开发 --> 10-工厂tcp
    01-08
```

```shell
#!/bin/bash
FILEINFO="./infotext"
sed -i `grep -n -w "${1}" $FILEINFO | awk -F : '{print $1}'`d $FILEINFO

//============================上面的能够运行,下面的不能运行(用$username 替代$1) 
#!/bin/bash
USERNAME=$1
FILEINFO="./infotext"
sed -i `grep -n -w "${1}" $FILEINFO | awk -F : '{print $USERNAME}'`d $FILEINFO

```



